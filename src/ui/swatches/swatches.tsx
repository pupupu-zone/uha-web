import React from 'react';

import Root from './swatches.styles';

import type { Props } from './swatches.d';

const Swatches = ({}: Props) => {
	return (
		<Root>
			<h1>Swatches</h1>
		</Root>
	);
};

export default Swatches;
